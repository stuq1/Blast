import {State} from "../../../core/stateMachine";

export class BoardWinState extends State {

    constructor(stateMachine) {
        super(stateMachine);
    }

    onActivate() {
        super.onActivate();
    }

    onDeactivate() {
        super.onDeactivate();
    }

    onEvent(event) {
        super.onEvent(event);
    }

}
